import {
    WixDataApi
} from '@wix/wix-data-core';
import {
    PlatformizedWixDataProvider,
} from '@wix/wix-data-platformized-provider';
const noopTraceWith = () => (fn) => fn();
export default function createWixData({
    cloudDataUrl = '/_api/cloud-data',
    gridAppId,
    segment,
    httpClient,
    authHeader,
    traceWith = noopTraceWith,
    allowGetRequests = false,
    shouldUseDataItemService = Promise.resolve(false),
}) {
    return new WixDataApi(new PlatformizedWixDataProvider({
        cloudDataUrl,
        gridAppId,
        segment,
        httpClient,
        authHeader,
        traceWith,
        allowGetRequests,
        shouldUseDataItemService,
    }));
}
//# sourceMappingURL=index.js.map